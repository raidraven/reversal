# 開発ログ(note商品化用の素材)

収益計画書([revenue-plan.md](./revenue-plan.md))Phase4-2「有料note(90日実況の全記録+使ったプロンプト全部)」のための素材ログ。
Claude Codeとのやり取りで「何を・なぜ・どう頼んだか」を開発のたびに追記していく。note執筆時はここから逆算して構成すればよい。

構成: 各エントリは `## 日付 タイトル` → 背景/課題 → 使ったプロンプト(要約 or ほぼ原文) → 何が起きたか → note向けのポイント(読者が学べること)。

---

## 2026-07-15より前 初期構築(パスワード再設定〜管理画面インライン編集)

- パスワード再設定機能(Resend連携)
- ログイン試行のレート制限をDBベースに変更 — Vercelのようなサーバーレス環境ではプロセスがリクエストごとに使い捨てられるため、メモリ上のレート制限カウンタが効かない不具合に遭遇。DBに移してリクエスト間で状態を共有した。
- 管理者向けインライン編集モード追加(サイト内のテキストをその場で編集できる`EditableText`コンポーネント)

**noteのポイント**: 「サーバーレスだとメモリ上のグローバル変数は使えない」は、AIに副業サイトを作らせる人が必ずぶつかる壁。ここでの実体験は再現性の高い教材ネタになる。

---

## 2026-07-15 コンパニオン(クロエ)のコスト最適化・サイトポリシー・運営バッジ

**背景**: AIキャラ「クロエ」との会話にClaude APIを使っているが、コスト管理と誇張しない誠実な人格設計が必要になった。

**やったこと**:
- コンパニオンの応答/モデレーション/質問生成でモデルを使い分け、プロンプトキャッシュを活用してコスト最適化
- 「誇張禁止(収益を保証しない・煽らない)」を明文化したサイトポリシーページを新設し、クロエの人格プロンプトにも反映
- 運営バッジ(管理者であることを示すUI)を追加

**noteのポイント**: 「AIキャラを競合と差別化する軸として"誠実さ"を明文化し、それをプロンプトにもUIにも一貫させた」という設計判断は、収益計画書の核(誇張しない実況型)と直結する。ここが「なぜこのサイトを作ったか」のストーリーの核になる。

---

## 2026-07-15 絵文字→Gemini生成アイコンへの全置換

**背景**: サイト内の見出し等に絵文字(🕯️🔑💬など)を仮置きしていたが、洋館×仮面舞踏会の世界観に対して安っぽく見えていた。

**やったこと**: Gemini(画像生成)でゴシック風のアイコン画像を生成し、`Icon`コンポーネント経由で絵文字を置き換え。`page.tsx`/`home/page.tsx`で`<Icon name="candle" />`のように使用。

**noteのポイント**: 「Claude Codeにコードを書かせる」だけでなく「他のAI(Gemini)で素材を作り、Claude Codeにそれを組み込ませる」というマルチAIワークフローの実例。副業ノウハウとして"無料〜低コストで世界観を統一する"具体策として使える。

---

## 2026-07-15 ログイン時のDB接続エラー誤表示バグ修正

**背景**: ログインに失敗した来賓から「パスワードが違う」というエラーが出るとの報告(または動作確認中に発見)。実際はDB接続エラーが発生していたのに、UIはそれを「パスワード間違い」として表示していた。

**何が起きたか**: 認証処理の例外ハンドリングが粗く、DB接続断とパスワード不一致を区別せず同じエラーメッセージに丸めていた。原因を切り分けて修正(コミット `dcaaa71`)。

**noteのポイント**: 「ユーザーに見えるエラーメッセージは、原因を潰さないと信頼を失う」という運用上の教訓。実況ログの「詰まった→直した」の典型例として使える(誠実さ路線とも整合)。

---

## 2026-07-15 モバイルUI改善・初回自己紹介・呼び方統一・管理画面の折りたたみ化

**背景**: 「館主」「運営」など呼称がページによってバラバラだった。モバイルでの操作感も詰めが甘かった。

**やったこと**:
- 「館主」→「主催者」に呼称統一、「談話室」などの命名も統一
- 来賓の初回自己紹介フローを追加
- モバイル用の右スライドメニュー(ヘッダーから開くハンバーガー的UI)を追加
- 管理画面の折りたたみ化(セクションごとにアコーディオン化して縦に長くなりすぎないように)

**noteのポイント**: 世界観もの(ロールプレイ系UI)は用語の一貫性が没入感を左右する。用語統一だけで体験が締まる、という小さいが効くリファクタの実例。

---

## 2026-07-15 サイト内の全ページに一貫したサイドメニュー・退会機能・LP統計の分離

**背景**: `/`(LP)にはサイドメニューがなく、ページごとにナビの持ち方がバラバラだった。また「来賓数」という統計が未登録の匿名訪問者と登録済みユーザーを混同していた疑いがあった。

**やったこと**:
- `layout.tsx`(全ページ共通)と`page.tsx`(LP専用)それぞれにナビを整備、`/articles`(書庫)へのリンクも追加
- 退会機能を実装
- LPの統計表示(`landingStats`)を「来賓数」(匿名含む)と「登録者数」に分離して誤解を招かないようにした

**noteのポイント**: 収益計画書のリスクメモにある「LP統計『来賓1名』等は少数期には逆効果」問題への布石。実数を正直に出す設計だからこそ、後で表示ロジックを変える判断も本音で書けるネタになる。

---

## 2026-07-15 来賓数の重複排除・プライバシーポリシー編集対応・サイドメニュー調整

**背景**: 来賓数カウントが同一訪問者を複数回カウントしてしまう不具合。プライバシーポリシーページも管理画面から編集できるようにしたい要望。

**やったこと**:
- `readAnonId`(匿名Cookie)とログインユーザーIDを使い、`visitorKey`でログイン中はアカウント単位、未ログインは匿名Cookie単位の重複排除キーを実装(`page.tsx`のコメント参照)
- プライバシーポリシーページを`EditableText`対応にして管理画面から編集可能に

**noteのポイント**: 「正確な指標を出す」ための小さいが地味に重要な実装(Server Componentは新規Cookieを発行できない制約への対処)。技術的に薄いが効く工夫の実例。

---

## 2026-07-15 SEO/マーケティング分析+90日ロードマップの策定

**背景**: サイトは技術的には完成度が高いが、収益導線・コンテンツ資産・独自ドメインがゼロという状態で「月1万円/90日」の目標をどう達成するか、という相談をした。

**使ったプロンプト(要約)**:
> 「このサイトの現状を総評してほしい。強み・弱みを整理した上で、月1万円を90日で達成するための現実的な収益計画書を作ってほしい。開発タスクはClaude Codeに任せる前提で、優先度・時間・依存関係も含めて」

**何が起きたか**: 収益モデルの逆算(アフィリ2件+有料note4部+AdSense)、診断(強み/弱み)、Phase1〜4のロードマップ、収益到達ルート、今すぐやるべきタスクTOP10を含む`docs/revenue-plan.md`を作成し、リポジトリに保存。あわせて記憶(memory)にも要点を保存し、今後のセッションでも参照できるようにした。

**noteのポイント**: これは note商品の骨格そのもの。「技術は作れるが売れない」という副業AI開発者が陥りがちな罠を、どうAIとの対話で言語化し、逆算のロードマップに落としたかというプロセスがそのまま教材になる。

---

## 2026-07-15 書庫(攻略記事)機能の実装

**背景**: 収益計画のPhase1最優先タスク「記事機能(書庫)の実装」。SEO流入の受け皿となる記事面が存在しないと検索から人が来ない。

**やったこと**: `/articles`配下にSSRで記事を配信できる書庫機能を実装(メタ情報・OGP込み)。LP(`page.tsx`)・ホーム(`home/page.tsx`)・共通レイアウト(`layout.tsx`)のナビに「書庫」へのリンクを追加。revenue-plan.mdのタスク1-8を完了マークに更新。

**noteのポイント**: 「機能を作った」で終わらせず、収益計画のタスクIDと紐付けて進捗管理している点が実況ものとして良い題材。次にやるべきは1-9(sitemap/robots.txt)であることも明確。

---

## 2026-07-15 収益計画の見直し(セルフバック削除)とASP登録の確認

**背景**: 「ASP登録は完了済みです」と報告を受けタスク1-2を完了に更新。続けて「セルフバックでは意味がない気がしますが、単にミッションを成功させるためにやるのですか?」という鋭い指摘があった。

**やり取りの要旨**: セルフバック(自己アフィリエイト)は一回性の収益で、フォロワーも記事も増えず月次収益目標への再現性がない。計画書にも元々「初期資金調達」と位置づけていたが、AIの側が率直に「タスク消化のためだけならやる必要はない」と認め、ユーザーの「削除して」の一言で計画書・KPI・収益到達ルートの3箇所から該当記述を除去した。

**noteのポイント**: AIが作った計画を無批判に実行せず、ユーザーが「これは本当に意味があるのか?」と検証し、AI側も迎合せず正直に非を認めて計画を修正した好例。「AIの提案は鵜呑みにしない」「必要ならAI自身に自己点検させる」という運用ノウハウとして使える。

---

## 2026-07-15 独自ドメイン(th-reversal.com)取得とVercel接続

**背景**: 収益計画タスク1-1。SEO上、独自ドメインなしでは不利になるため最優先で着手。

**使ったプロンプト・やり取りの流れ**:
1. 「リバーサルに寄せて考えて、SEO効果が高いものにしたい」→ ドメイン名の候補出し(EMD=完全一致ドメインのSEO効果は現在ほぼ無いという前提を正直に説明した上で、ブランド一貫性重視で候補を提示)
2. ユーザーが「th-reversal.com」を独自に取得
3. 「今のレンタルサーバー設定はどうなってる?」→ Vercelでホストされておりレンタルサーバーではないこと、DB(Postgres/Prisma)・NextAuth・Resend・Claude APIの構成を説明
4. 「th-reversal.comでドメイン発行しました」→ `vercel domains add`でVercelプロジェクトに追加(apex + www)
5. DNS未反映のトラブルシューティング — `nslookup`で権威DNSサーバーに直接問い合わせて確認しながら進行。原因は「ドメインのネームサーバー設定がレンタルサーバー(RSプラン)側を向いており、お名前.comの『ドメインDNS』ではなく『サーバーDNS』側の編集が必要だった」という、よくあるが分かりにくい罠
6. MXレコード(お名前メール)が既に存在していたため、ネームサーバー丸ごと切り替え(方法A)ではなく、既存ゾーン内でAレコード/CNAMEのみ書き換える方法B(低リスク)を選択
7. スクリーンショットを見ながら「ここをクリックしてください」を繰り返し、Aレコード書き換え→CNAME追加→`vercel domains verify`で最終確認
8. `NEXTAUTH_URL`環境変数の本番反映は、Claude Codeの自動実行ガードが「本番設定の未承認変更」としてブロック → ユーザーに明示確認を取ってから実行
9. 再デプロイ後、ブラウザで実際に`https://th-reversal.com`が表示されることを確認して完了

**つまずいたポイント(note向けに特に有用)**:
- ドメイン購入直後、DNSレコードを正しい場所で編集しないと何時間待っても反映されない(「ドメインDNS」と「サーバーDNS」が別物というGMO系レジストラ特有の罠)
- `nslookup <ドメイン> <権威サーバー>`で直接問い合わせると、リゾルバキャッシュを除外して「本当に反映されているか」を切り分けられる
- メール(MXレコード)が既にある場合、ネームサーバーを丸ごと切り替えるとメールが壊れるリスクがあるため、A/CNAMEだけを個別に書き換える方が安全
- Windows環境の`curl`は証明書失効チェック(schannel)でSSL自体は正常でもエラーになることがある(`--ssl-no-revoke`で切り分け)
- 本番環境変数の変更は、AI側の安全装置が自動実行をブロックする設計になっており、ユーザーの明示的な承認が必須だった

**noteのポイント**: 「ドメイン接続」は技術ブログでは1行で終わりがちだが、実際は非技術者がつまずく最大のポイントの一つ。スクリーンショットを見せながら一つ一つ確認していくプロセスそのものが、note読者(非エンジニアの副業実践者)向けの再現性が高い教材になる。「AIが本番変更を勝手に実行しない」という安全性の実例としても使える。

---

## この後の記録方針

- 今後、機能追加・バグ修正・戦略転換などの区切りごとにこのファイルへ追記する
- 各エントリは「背景→使ったプロンプト→結果→noteのポイント」の型を維持する
- revenue-plan.mdのタスク完了時は、このログにも対応するエントリを追加する
